

<!-- If @board.public && user is NOT logged in
	show only public boards
	else
	only show boards by that user -->

<% if session[:current_user_id]  %>
	<%= render @boards %>
<% else %>
	<%= render partial: 'board', locals: {board: @board, public_board: true} %>
<% end %>